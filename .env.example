# Resend API key for the contact form. Use "test" locally/CI for the mock send path.
RESEND_API_KEY=test

# Site URL (no trailing slash). Server-only; used for canonical/OG metadata.
SITE_URL=https://www.mividadigital.co.uk

# Sender address — must be on a domain/subdomain verified in Resend.
CONTACT_FROM_EMAIL=hello@contact.mividadigital.co.uk

# Where contact enquiries are delivered.
CONTACT_TO_EMAIL=hello@mividadigital.co.uk

# Google Analytics 4 Measurement ID. Client-exposed (NEXT_PUBLIC_*).
# Analytics only loads after the visitor accepts cookies. Leave blank to
# disable GA entirely (e.g. in preview/staging environments).
NEXT_PUBLIC_GA_ID=G-009MTHBS83
